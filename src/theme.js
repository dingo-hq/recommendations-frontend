import { defaultTheme } from 'evergreen-ui';

export default {
    ...defaultTheme,
    fontFamilies: {
        ...defaultTheme.fontFamilies,
        display: 'Sharp Grotesk',
        ui: 'Sharp Grotesk',
    },
};
